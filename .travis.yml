language: cpp
sudo: required
dist: focal
compiler:
- clang
os:
- linux
before_install:
  - sudo apt-get -y install ninja-build
script:
- mkdir build && cd build && cmake .. -G Ninja && ninja && CTEST_OUTPUT_ON_FAILURE=1 ninja test
